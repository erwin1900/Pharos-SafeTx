# Install Codex Skill

Copy this folder to your Codex skills directory:

```bash
mkdir -p "$HOME/.codex/skills/pharos-safetx"
cp SKILL.md "$HOME/.codex/skills/pharos-safetx/SKILL.md"
```

Then restart or reload Codex and trigger with:

```text
$pharos-safetx run demo
```
